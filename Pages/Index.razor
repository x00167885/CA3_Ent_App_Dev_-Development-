@page "/"
@inject NavigationManager NavigationManager

<PageTitle>Loading...</PageTitle>

@code {
    // Redirecting to the proper main page of the website. (Fixes NavBar highlighting on link select.)
    protected override async Task OnInitializedAsync()
    {
        NavigationManager.NavigateTo("/pictureLookup");
    }
}